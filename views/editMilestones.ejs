<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        background-color: rgb(229, 229, 229);
        margin: 0;
        padding: 20px;
        color: white;
      }

      h1 {
        color: #ff5555;
        text-align: center;
        margin-bottom: 20px;
      }

      form {
        background-color: #ffffff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        max-width: 400px;
        margin: 0 auto;
      }

      label {
        display: block;
        margin-bottom: 8px;
        color: #ff5555;
      }

      input {
        width: 100%;
        padding: 10px;
        margin-bottom: 15px;
        box-sizing: border-box;
        border: 1px solid #ff5555;
        border-radius: 4px;
        background-color: rgb(229, 229, 229);
        color: black;
      }

      button {
        background-color: #ff5555;
        color: white;
        padding: 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        width: 100%;
      }

      button:hover {
        background-color: #ff3333;
      }
    </style>
  </head>
  <body>
    <h1>Edit Milestone</h1>
    <form action="/milestones/update/<%= milestone.id %>" method="post">
      <label for="year">Year:</label>
      <input
        type="text"
        name="year"
        value="<%= milestone.year || '' %>"
        required
      />

      <label for="title">Title:</label>
      <input
        type="text"
        name="title"
        value="<%= milestone.title || '' %>"
        required
      />

      <label for="description">Description:</label>
      <input
        type="text"
        name="description"
        value="<%= milestone.description || '' %>"
        required
      />

      <label for="photourl">Photo URL:</label>
      <input
        type="text"
        name="photourl"
        value="<%= milestone.photourl || '' %>"
        required
      />

      <button type="submit">Update Milestone</button>
      <button style="margin-top: 10px; background-color: #777777">
        <a href="/milestones" style="text-decoration: none; color: white"
          >Go back</a
        >
      </button>
    </form>
  </body>
</html>
