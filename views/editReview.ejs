<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        background-color: rgb(229, 229, 229);
        margin: 0;
        padding: 20px;
        color: white;
      }

      h1 {
        color: #ff5555;
        text-align: center;
        margin-bottom: 20px;
      }

      form {
        background-color: #ffffff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        max-width: 400px;
        margin: 0 auto;
      }

      label {
        display: block;
        margin-bottom: 8px;
        color: #ff5555;
      }

      input {
        width: 100%;
        padding: 10px;
        margin-bottom: 15px;
        box-sizing: border-box;
        border: 1px solid #ff5555;
        border-radius: 4px;
        background-color: rgb(229, 229, 229);
        color: black;
      }

      button {
        background-color: #ff5555;
        color: white;
        padding: 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        width: 100%;
      }

      button:hover {
        background-color: #ff3333;
      }
    </style>
  </head>
  <body>
    <h1>Edit Review</h1>
    <form action="/reviews/update/<%= review.review_id %>" method="post">
      <label for="employee_id">Employee ID:</label>
      <input
        type="text"
        name="employee_id"
        value="<%= review.employee_id || '' %>"
        required
      />

      <label for="review_text">Review Text:</label>
      <input
        type="text"
        name="review_text"
        value="<%= review.review_text || '' %>"
        required
      />

      <label for="review_date">Review Date:</label>
      <input
        type="date"
        name="review_date"
        value="<%= review.review_date || '' %>"
        required
      />

      <button type="submit">Update Review</button>
      <button style="margin-top: 10px; background-color: #777777">
        <a href="/reviews" style="text-decoration: none; color: white"
          >Go back</a
        >
      </button>
    </form>
  </body>
</html>
