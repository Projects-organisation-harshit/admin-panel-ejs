<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Cause</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        background-color: #f0f0f0;
        margin: 0;
        padding: 20px;
      }

      h1 {
        color: #333;
        text-align: center;
        margin-bottom: 20px;
      }

      form {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        max-width: 400px;
        margin: 0 auto;
      }

      label {
        display: block;
        margin-bottom: 8px;
        color: #555;
      }

      input {
        width: 100%;
        padding: 10px;
        margin-bottom: 15px;
        box-sizing: border-box;
        border: 1px solid #ccc;
        border-radius: 4px;
      }

      button {
        background-color: #4caf50;
        color: white;
        padding: 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        width: 100%;
      }

      button:hover {
        background-color: #45a049;
      }
    </style>
  </head>
  <body>
    <h1>Edit Cause</h1>
    <form action="/causes/update/<%= causes.id %>" method="post">
      <label for="title">Title:</label>
      <input type="text" name="title" value="<%= causes.title %>" required />

      <label for="raised_money">Raised Money:</label>
      <input
        type="number"
        name="raised_money"
        value="<%= causes.raised_money %>"
        required
      />

      <label for="goal">Goal:</label>
      <input type="number" name="goal" value="<%= causes.goal %>" required />

      <label for="photourl">Photo URL:</label>
      <input type="text" name="photourl" value="<%= causes.photourl %>" />

      <button type="submit">Update Cause</button>
    </form>
  </body>
</html>
